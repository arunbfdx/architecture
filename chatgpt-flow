flowchart TD
    %% UI Layer
    UI[User Interface\nReact (Web), Swift (iOS), Kotlin (Android), Electron (Desktop)]

    %% Orchestration Layer
    ORCH[Conversation Orchestrator\nPython/Go backend, prompt assembly, routing]

    %% NLU Layer
    NLU[Natural Language Understanding\nGPT model for intent/entity extraction]

    %% Memory Layer
    MEM[Memory / Context Manager\nShort-term session memory, Vector DB]

    %% Knowledge Access Layer
    RAG[Knowledge Access (RAG)\nEmbeddings + Vector DB, Web Search]

    %% Action Layer
    TOOL[Action Layer (Tools / Plugins)\nCode Interpreter, APIs, File handling]

    %% NLG Layer
    NLG[Natural Language Generation\nGPT-4o / GPT-5 response generation]

    %% Moderation Layer
    MOD[Moderation Layer\ntext-moderation-latest, Rule filters]

    %% Output Layer
    OUT[Output Renderer\nFormatted chat response back to UI]

    %% Flow connections
    UI --> ORCH
    ORCH --> NLU
    NLU --> MEM
    MEM --> RAG
    RAG --> TOOL
    TOOL --> NLG
    NLG --> MOD
    MOD --> OUT
    OUT --> UI
